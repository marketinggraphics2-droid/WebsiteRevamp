<?php
/**
 * Single testimonial for the theme's own post type (/testimonials/<client>/) — same page as
 * the live site's customer_testimonial single: H1, previous / next arrows, quote + video.
 *
 * @package dynamiqes
 */

require get_template_directory() . '/single-customer_testimonial.php';
